import styled from 'styled-components';
  
export const Button = styled.div`
   position: fixed; 
   width: 100%;
   left: 50%;
   bottom: .2em;
   height: 2em;
   font-size: 3rem;
   z-index: 1;
   cursor: pointer;
   color: rgb(70,130,180);
`