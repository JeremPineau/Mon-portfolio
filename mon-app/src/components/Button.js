import styled from 'styled-components';
  
export const Button = styled.div`
   position: fixed; 
   width: 20%;
   left: 50%;
   z-index: 1;
   cursor: pointer;
   color: rgb(70,130,180);
   font-size: 4vw;
   bottom: 3vw;
   opacity: 70%;
`